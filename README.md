# Chayan - Deterministic Agent Selection Engine

## Overview
This project implements a deterministic agent selection system that maps tasks to predefined agent IDs.

No machine learning or UI is used. The system is purely rule-based and produces consistent outputs.

## How to Run
python main.py

## Test Cases
python test_cases.py